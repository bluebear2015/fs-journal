# Foundations of Web Development
01. In your own words, why do we use Git?
    > | 
    We use the git repository to synch and connect with other people working on the project...so more in my own words, its an coding confrence call.  
    |

02. In the terminal, what is the command `mkdir` used for?
    > | 
    It is used to make new files inside of whatever file you are currently in
     |

03. What is a ***pseudo-class*** and what are some of the most common ones you think you will use?
    > | 
    It overrides the default commands. not quite sure
    |

04. What is ***specificity*** and how might you use it to your benefit?
    > | 
    you can use specificity to let your css know what class is most relevent to the class  
     |

05. What problems do you think you could run into if you over-utilized the `!important` feature?
    > | 
    you could start overriding already working elemnt on the page.
     |

06. What are the three components that makeup a `CSS` rule? <br> Example:

    ```css
        h1 {
            color: rgba(255, 210, 33, .75);
        }
    ```

    > | 
    the class id that your calling on, what function you want the selected id to do, and the value for the function
    |

07. How do you think good design influences people when visiting a website, and what sorts of things could you convey through design alone?
    > | 
    it catches the eye by looks, but also has the reason your on the site present and easy to find, not confusing with to many things happing at once
    |

08. What is the purpose of ***wireframing***?
    > | 
    it is to set the bones of the project so you know where you heading with it
     |

09. Do you think wireframes are worth the time, energy, and effort that they require? Why or Why not?
    > | 
    it allows us to trully understand where each needed component is befor styling
    |

10. Define the display `:flex property:`
    > | 
    flex allows you to break the standard html format and lets align side to side other then up and down
    |

11. What `CSS` properties affect the size of a box model?
    > | 
    the margin??
     |
