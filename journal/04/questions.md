# Understanding Asynchronous Code, and API's
01. What is the difference between `asynchronous` code and `synchronous` code?

  > | asynchronus code allows us to pause and wait before it runs the rest of the code.  |

02. What is an event listener?

  > | A event listener listens for any changes in the appstate and updates functions and the webpage as needed. |

03. What does *REST* stand for, and in simple terms what does it mean??

  > | a giuedline to manage communication |

04. What is a callback / higher order function?

  > | async and await |

05. What is a `promise`? How do you capture an error from a `promise`?

  > |a promise is a class that takes in a function that can be resolve or rejected. Using a try catch we can make sure we are getting the promise fulfilled before running the rest of the function |

06. Name three processes used to make requests over `HTTP`?

  > | try catch. pop error. and await. |

07. What does the `API` acronym stand for?

  > | application program interface |

08. What must you do in order to `await` a promise inside of a function?

  > | it needs to have an async in front of it so its waiting on a specific action to happen befor it runs the rest of the code. it also needs to have a try catch as well. |

09. What is the purpose of encapsulation in programming?

  > | to keep access limited to what only needs to be limited to. |

10. What is `HTTP` response code for a successful request?

  > | 200 |

11. What is a 400 error?

  > | malformed URL |
