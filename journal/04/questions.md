# Understanding Asynchronous Code, and API's
01. What is the difference between `asynchronous` code and `synchronous` code?

  > | 
  Async is something this is working individual of the appstate
  synchronous is something that happens as soon as the page loads
  |

02. What is an event listener?

  > | 
  an event listener is something that is listing for any changes in the appstate and will act according to those changes
   |

03. What does *REST* stand for, and in simple terms what does it mean??

  > | ANSWER HERE |

04. What is a callback / higher order function?

  > | 
  a callback is a method that is watching over another method and if the original methods terms are met the second methods terms will take effect and so on... 
  |

05. What is a `promise`? How do you capture an error from a `promise`?

  > | ANSWER HERE |

06. Name three processes used to make requests over `HTTP`?

  > | 
  we need to relay it through our envs file and the router
  |

07. What does the `API` acronym stand for?

  > | ANSWER HERE |

08. What must you do in order to `await` a promise inside of a function?

  > | 
  it is awaiting the action of another method or function
  |

09. What is the purpose of encapsulation in programming?

  > | 
  encapsulation is keeping files containing certain functions that perform individual tasks seperate
   |

10. What is `HTTP` response code for a successful request?

  > | ANSWER HERE |

11. What is a 400 error?

  > | 
  its is not able to access the objest or array you are requesting
  
   |
