# Application Architecture, MVC Design Pattern
01. What are the Pillars of Object Oriented Programming (`OOP`)?
  
  > | 
  encapsulation; one class does not have access to data from a different class 
  inheritance; it takes a set class and lets you add more things to that class. 
  abstraction; keeping classes in seperate files for accesibility
  
  |

02. How does `export` differ from `export default`?
  
  > | 
  export default only lets you export on value, 
  while export will let you export multiple values
  
  |

03. What is Encapsulation?
  
  > | 
  will keep classes and values seperate unless theres a method of extraction
   |

04. What are some of the benefits of the `Proxy` object that we are using in our structure for applications?
  
  > | 
  keps things private.
  its what we do in the model when set a this.data
  |

05. What the difference between a `class` and an instance of a `class`?
  
  > | 
  so you can take a class and essientially treat it like an object to make a new class.
   |

06. What is a computed Property?
  
  > | 
  i understand it to be classes
  |

07. What is the purpose of the `MVC` pattern?
  
  > | to keep things serperate and organized use for big project with|

08. What is the job of the `Controller` in the `MVC` Pattern?
  
  > | 
  controls the way a user can interact with the application
   |

09. What is the job of the `Service` in `MVC`?
  
  > | 
  service contains the buisness logic and manipulating data
  |

10. What is the job of the `Model` in `MVC`?
  
  > |
  holds the models for the page
  |
