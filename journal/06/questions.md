# Single Page Applications with Vue
01. What is the entrypoint of an application?

  > | 
  identifies a resource that is an access point to an application
  |

02. What is the difference between a vue `component` and `page`?

  > | 
   a Vue is the pages.vue, while a component refers to something that is reusable and can be used in a view?
   |

03. What is ***Component-Based Architecture***?

  > | 
  framework for building software based on reusable components. Each component is encapsulated
   |

04. What are the three tags that make up a Vue component?

  > | 
  components must include a template tag and a script. A style tag.
   |

05. What are ***lifecycle hooks***? What are lifecycle hooks used for?

  > | 
   provides the amount of time to wait for the action to complete before the instance is pushed to the next state like a watcher method?
   |

06. Which component in Vue does the vue-router use to mount pages onto?

  > | 
  the vueport and the router link......? 
   |

07. What is the difference between the `AppState` and the state object within a component?

  > | 
  te appstate is only storing the info for the instance a user is logged in, so itd local storage
   |

08. What is the responsibility of `Services` in our Vue projects?

  > | 
  it is responsible for creating and developing user buisness logic that doesnt need to be in the applications by using Vue 
   |

09. What are ***props*** and how are they used? Provide an example

  > | 
  stands for properties and is being used for passing data from one component to another
  |

10. What is the Vue method used to create watchable objects such as `state` or `AppState`?

  > | 
  onMounted. and on compute
   |
