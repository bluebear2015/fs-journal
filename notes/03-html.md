# HTML

### HTML BONES
//NOTE - 

-The head is reserved for meta data and deliviring info to the browser
-the title tag is for the actuall tab name on the browser
-<href> is used for adding/changing the tab pic
-the <body> holds most of what the page displays including the header at the top and footer at the bottom
-we build stucture with tags, and then we put the info into them
-inside the body is where the tags are ex. header, main
-<main> should be most of the page
-inside the main is where we can make <section> to layout the page
-inside the sections is were we can add <div> to call apon during further styling 
-html structure is key before you start anything
-the html structure should be able to read easlily
-<link>.stlye.css to link to css
-root is a sudo element that everything has accsess to
-after root styles have been made we can start to target the body and different classes calling on styles using var()
-The flex CSS shorthand property sets how a flex item will grow or shrink to fit the space available in its flex container
-flex-grow only works when theres a set heighth
-LEARN MORE ABOUT FLEX TONIGHT!!
-div*any number will make that amount of divs
-flex alows us to push what we wnat either horizontally of vertically
-the parent element is what you flex if you want to target multiple things inside an element
-LEARN HOT KEYS FOR MAC IN VS CODE!!
-flex and aligne tools get implemented inside css .classname{flex}
-inside devtools we can find a flex option list
-make sure to use - when stringing words togther ex. sam-luna
-if you want spacing between your element click on them inside dev tools and add margin & padding 
-CODE ORGANIZATION IS KEY
-Use rem for heighth and width
-rem go off of root height and width
-.justify-content-between to evenly spread out content
u-se <span> inside a div to add content
-in dev tools we can access the gap tool inside styles, and add gep between content
-make sure to give most enerything a name to call on in css
- assign class active-link and call on in in css to make a hyperlink
-<div> will take up a whole blosk <span> only goes to text length
-use lorem*number to make fake text equalling the number
-use <p> to make paraghraghs
-<button> to make clickable buttons
-using flex isnt enough, you need to set width and heighth as well
-images if not restricted will take up 100% of the parent element
-% are how you set the width 100% being the whole page
-margin right or margin left inside css to add space oon the sides
-box-sizing sets how the total width and hiegth are calculated
-border-box is needed for calculating the border and the width
-width used % margin uses rem heigth uses rem border radius uses px
-any elements that you are using frequently make it a var inside of the root{}
-font-weight controls how thick or thin the text is
-transitions allow subtle color changes like when hovering over a button
-filter allows effects like blur or color shift to an element
-drop shadow allow a traspariet effect to occour to the element
-the A tag creats a quick hyperlink
-you need to set a heighth when an image has been added to the code
-

//NOTE - 