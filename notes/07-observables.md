# Observables

use a sync to tell a function to wait 
in the dev tools you can see requests made on data in the network tab
const res = await api.get()
res is the response from the other computer
.map takes an idividual item and plae it in the 
.map(c => charecters(c))
