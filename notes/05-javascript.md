# JavaScript
-use modulus operators % = multiply     

-after a conditional has been stated use ? 'even'(what to do when true) : 'odd'(what to do when false)

-<script scr = "app.js"> inside the bottom of body
-jave has certain data types like:
STRINGS
-java has certain quotes that you can use to denote string
such as 
""; double quotets plus a terminator ; 
'' ; single quots
 
 NUMBERS
 -1
 0
 1
 0.2

javascript is not good at math

BOOLEAN
booleans are true or false nothing else use boolean when you wanta function to do something when true or false
let *object* = *true or false*
then we can use an IF statement

weird ones:
NULL
UNDEFINED 
NaN

OBJECTS
objects are denoted by {}; or object
 
ARRAYS arrays are denoted wth []; or array

*LECTURE PROJECT*

use 'let' inside java to create an object
after let has declared somthing you can use if statement

wrap individual functions in a function tag
let heroHealth = 100
let has HealthPotion = true

you can put else inside of your if statement

if(hasHelalthPotion){
    heroHealth += 50
    hasHealthPotion = false
}
you can throw an error to stop a function or return alerts
 it is easiest to make your fail conditions first so any condition that will stop functionality

 *!* means not or false
 LOOK UP WHAT BOILERPLATE IS

 how to make it into a web page you can add a attribute of on click the same way you add a class

 you can copy and paste the names of funtions
 use -= to degrade your set number
 math.floor gives us whole number
 use console.log to test functions befor we apply it
be good about labling your console logs
span keeps things inline together
 inside of a funtion you can do document.getElelmentById(className).textcontent =
 ${heroHealth > 0 ? heroHealth : 'dead'}
 you can put function inside of a function


                                                                TUESDAY!!!

use class=display to creat a top banner for the page.
🥸 use controle command space to open emojies for page 🤩
you can override bootstrap button classes by using !important so you can make them different sixe
you can you :first-child to target the first thing in a parent class
you can do less than 1rem in css by using .#
 ///////!SECTION

 fwhen you write a fuctions need a argument inside of the /function function name(argument)/
 try to always call thing what they are when naming things
 
                                                !keywords! 
 
 string:
 
 number:
 
 boolean:
 
 object:
 
 array:

/LOOK UP AND DEFINE/ when is it a parameter versus when its an argument

Parameters are variables listed as a part of the function definition. 
Arguments are values passed to the function when it is invoked.

[] makes it an array arrays work at a postion based output
use commas inside of an array to serrerate
null is esentially a place holder
name of array[position in the array]
to get to certain propertie in side a statement use name.propetie
parameters are what you define when you make a function

handle functionssay go do something else

ul unlist
li list items
return means full stop after function has been performed

to get random
fucntion functionname(){
array.length /will scale everything inside of the called pon array
const randomIndex = math.floor(math.random() * array)
murders = array[randomIndex]

break statement terminates the current loop or switch statement and transfers program control to the statement following the terminated

if(arayName.ObjectName && functionName == 'object inside of the array')

givenClues.push(cluekey)

we can use switch(cluekey){
    case 'weapon':
    if(murderer.weapon== "teeth"){
        return drawclue('message')

    }
}

let name[                                                      vv esentialy a if else statement
    you can string together if statement by diiding them with : ? to tell the function to something if the first thing is met if it doesnt it will move on to the next thing to see if the parameters are met

}

 let argumentname = 'arrayName'
LET objects inside of the array = {
    NAME: "craig",
    picture: "jk",
    oceanbound: true,
    diet: ['meat','fruit']
    age: 823
}
]

let functionName [items,listed,in,the,array]

function functionName(){
    const options = [items,in,the,arrary]
const clueKey = option[randomOptionIndex]
if(givenClue.includes(cluekey))

}

//            vv array/vv parameter
 function accuseAnimal(animal){
 if(animal == muderer){
    alert ('great job detective')

return

if(array.name == 'properties in side objects in the array)

 }
console.log("the murderer will strike again")
 }

function drawHint(){
    document.getelementbyid().innerhtml += /name what class or id you want to putt text on with in html/
}





onclick = 'on-click = accuseAnimal(squid)'




                                           ----
                                          


